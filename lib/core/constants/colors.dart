import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary
  static const Color primary = Color(0xFF5046E5); // Indigo
  static const Color secondary = Color(0xFFFF6F3D); // Orange
  static const Color accent = Color(0xFFFF6F3D);

  // Backgrounds
  static const Color background = Color(0xFFF9F9FB);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF0A0B1E);
  static const Color textSecondary = Color(0xFF6B7280);
  
  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444); // Standard Red

  // Borders & Dividers
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}
